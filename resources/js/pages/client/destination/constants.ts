import { getImageUrl } from "@/lib/utils";

export const SECTION_DELAYS = Object.freeze({
    destinationSection: 0.05,
    inboundSection: 0.10,
    outboundSection: 0.15,
});



export const DESTINATION_HERO_IMAGE = getImageUrl('upload/agency/destination_image.png');

